import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '@/components/ui/card';

export default function CanteensPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Canteens</CardTitle>
        <CardDescription>
          Manage all canteens on the platform.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Canteen management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
